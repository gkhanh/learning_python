name: Octo Organization CI

on:
  push:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: install py
        run: sudo apt-get install python-pip 

      - name: run basic_functions
        run: py basic_functions.py
